// Placeholder API service. Replace base URL with your backend.
const BASE = process.env.REACT_APP_API_URL || 'http://localhost:5000';

export async function fetchPatients(){
  // sample: return fetch(BASE + '/patients').then(r=>r.json());
  return [];
}
